---
import { education } from "../data/education";
---

<div class="flex flex-col gap-4">
  <h2 class="font-serif font-semibold text-2xl sm:text-3xl">Education</h2>
  <div class="border border-border rounded-3xl p-6 sm:p-10 bg-card">
    <div class="relative border-l border-border space-y-12 pl-8 py-2">
      {
        education.map((edu) => (
          <div class="relative">
            <div class="absolute -left-[44px] top-1 flex h-6 w-6 items-center justify-center rounded-full bg-background border border-border">
              <div class="h-2 w-2 rounded-full bg-muted-foreground" />
            </div>
            <div class="flex flex-col gap-1">
              <span class="text-sm font-mono text-muted-foreground">
                {edu.startYear} - {edu.endYear}
              </span>
              <h3 class="text-lg font-semibold text-foreground">
                {edu.school}
              </h3>
              <p class="text-base font-medium text-muted-foreground">
                {edu.degree}
              </p>
              {edu.description && (
                <p class="mt-2 text-sm text-muted-foreground max-w-prose leading-relaxed">
                  {edu.description}
                </p>
              )}
            </div>
          </div>
        ))
      }
    </div>
  </div>
</div>
